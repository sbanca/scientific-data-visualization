using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Augmentations : MonoBehaviour

{

    Camera mainCam ;

    // projected head direction ray 
    void Start()
    {
        mainCam = Camera.main;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
